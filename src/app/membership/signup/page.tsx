import SignupForm from "./SignupForm";

export const metadata = {
  title: "สมัครสมาชิก — err.day",
};

export const dynamic = "force-dynamic";

export default function MembershipSignupPage() {
  return <SignupForm />;
}
