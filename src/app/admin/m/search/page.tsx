import SearchView from "./SearchView";

export const revalidate = 30;
export const metadata = { title: "ค้นหา — Mobile Admin" };

export default function MobileSearchPage() {
  return <SearchView />;
}
